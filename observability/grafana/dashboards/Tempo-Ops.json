{
  "annotations": {"list": []},
  "editable": true,
  "graphTooltip": 0,
  "links": [],
  "panels": [
    {
      "type": "stat",
      "title": "Exporter Send Failures (5m rate)",
      "gridPos": {"h": 5, "w": 8, "x": 0, "y": 0},
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "targets": [{"expr": "sum(rate(otelcol_exporter_send_failed_spans[5m]))", "refId": "A"}]
    },
    {
      "type": "stat",
      "title": "Exporter Sent Spans (5m rate)",
      "gridPos": {"h": 5, "w": 8, "x": 8, "y": 0},
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "targets": [{"expr": "sum(rate(otelcol_exporter_sent_spans[5m]))", "refId": "A"}]
    },
    {
      "type": "stat",
      "title": "Receiver Refused Spans (5m rate)",
      "gridPos": {"h": 5, "w": 8, "x": 16, "y": 0},
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "targets": [{"expr": "sum(rate(otelcol_receiver_refused_spans[5m]))", "refId": "A"}]
    },
    {
      "type": "timeseries",
      "title": "Exporter Queue Size by Exporter",
      "gridPos": {"h": 9, "w": 24, "x": 0, "y": 5},
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "targets": [
        {"expr": "sum by(exporter)(otelcol_exporter_queue_size)", "legendFormat": "{{exporter}}", "refId": "A"}
      ]
    },
    {
      "type": "timeseries",
      "title": "Receiver Accepted vs Refused",
      "gridPos": {"h": 9, "w": 24, "x": 0, "y": 14},
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {"unit": "1/s"}},
      "targets": [
        {"expr": "sum(rate(otelcol_receiver_accepted_spans[5m]))", "legendFormat": "accepted", "refId": "A"},
        {"expr": "sum(rate(otelcol_receiver_refused_spans[5m]))", "legendFormat": "refused", "refId": "B"}
      ]
    }
  ],
  "refresh": "30s",
  "schemaVersion": 39,
  "style": "dark",
  "tags": ["systemupdate", "tempo"],
  "templating": {"list": []},
  "time": {"from": "now-6h", "to": "now"},
  "title": "Tempo Ops",
  "uid": "tempo-ops"
}
