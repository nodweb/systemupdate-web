# Configuration

Backend environment (examples):
- SECRET_KEY, JWT_SECRET_KEY, DEVICE_ENROLLMENT_KEY
- DATABASE_URL, REDIS_URL
- CORS_ORIGINS
- SOCKETIO_* (async mode, CORS)

Frontend:
- VITE_API_URL, VITE_WS_URL

Android (BuildConfig):
- C2_URL, C2_HOST, PIN1, PIN2
