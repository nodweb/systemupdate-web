# نصب و راه‌اندازی

پیش‌نیازها: Docker، Node 20+، Python 3.11+.

```bash
cp .env.example .env
docker compose up -d
```
Frontend: http://localhost:3000
Backend Health: http://localhost:5000/health
جزئیات ویندوز: ../WINDOWS_SETUP.md
